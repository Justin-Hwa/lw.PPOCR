# PP-OCRv6 Medium ONNX Operator Analysis (V0)

> Generated by `converter/analyze_onnx.py`. Do not edit operator counts by hand.

## Scope and decision boundary

This report describes only the exact PP-OCRv6 medium model graphs supplied to the analyzer.
It is an analysis report only; it does not promote Medium to the production runtime or public C ABI.
FP32, CPU, scalar, and single-threaded. ONNX is a converter dependency only.

## Model inventory

| Model | SHA-256 | Opset | Nodes | Initializers | Weight bytes | Input | Output | Representative FLOPs |
|---|---|---:|---:|---:|---:|---|---|---:|
| DET | `eb13b44b25bb36f89528b68720af8a61d9cf381176107f465db1757b65d086e1` | ai.onnx:14 | 276 | 226 | 61,946,560 | `x` [DynamicDimension.0, 3, DynamicDimension.1, DynamicDimension.2] | `fetch_name_0` [ConvTranspose_592_o0__d0, 1, ConvTranspose_592_o0__d2, ConvTranspose_592_o0__d3] | 69.781 GFLOPs |
| REC | `9c09abf0957f7968c7586464b7397b84ad2387a0497a351af40e9acc71b673ba` | ai.onnx:11 | 508 | 254 | 76,461,256 | `x` [DynamicDimension.0, 3, 48, DynamicDimension.1] | `fetch_name_0` [DynamicDimension.0, Reshape_498_o0__d2, 18710] | 11.292 GFLOPs |

Representative shapes are DET `[1,3,640,640]`, CLS `[1,3,80,160]`, and
REC `[1,3,48,320]`. FLOPs count multiply-add as two operations and exclude
pre/post-processing and elementwise costs, so they are estimates rather than benchmark data.

## Operator union (27 types)

| Operator | DET | REC | Required by |
|---|---:|---:|---|
| Add | 59 | 109 | DET, REC |
| AveragePool | 0 | 1 | REC |
| BatchNormalization | 0 | 3 | REC |
| Concat | 2 | 3 | DET, REC |
| Conv | 122 | 62 | DET, REC |
| ConvTranspose | 2 | 0 | DET |
| Div | 13 | 19 | DET, REC |
| Erf | 13 | 14 | DET, REC |
| HardSigmoid | 5 | 6 | DET, REC |
| Identity | 0 | 143 | REC |
| MatMul | 0 | 13 | REC |
| MaxPool | 1 | 1 | DET, REC |
| Mul | 31 | 46 | DET, REC |
| Pow | 0 | 5 | REC |
| ReduceMean | 5 | 16 | DET, REC |
| Relu | 16 | 11 | DET, REC |
| Reshape | 0 | 8 | REC |
| Resize | 6 | 0 | DET |
| Shape | 0 | 4 | REC |
| Sigmoid | 1 | 5 | DET, REC |
| Slice | 0 | 8 | REC |
| Softmax | 0 | 3 | REC |
| Sqrt | 0 | 5 | REC |
| Squeeze | 0 | 8 | REC |
| Sub | 0 | 5 | REC |
| Transpose | 0 | 9 | REC |
| Unsqueeze | 0 | 1 | REC |

## Per-model findings

### DET

Operators: `Add` × 59, `Concat` × 2, `Conv` × 122, `ConvTranspose` × 2, `Div` × 13, `Erf` × 13, `HardSigmoid` × 5, `MaxPool` × 1, `Mul` × 31, `ReduceMean` × 5, `Relu` × 16, `Resize` × 6, `Sigmoid` × 1.

- Dynamic tensors after ONNX shape inference: 277.
- Shape/metadata nodes: 0.
- Constant-fold candidates: 0.
- Identity nodes removable by the converter: 0.
- Conv + BatchNormalization fusion candidates: 0.
- Estimated heavy-operator work: 69.781 GFLOPs.
- FLOPs by heavy operator: Conv 66.422 GFLOPs, ConvTranspose 3.359 GFLOPs.

### REC

Operators: `Add` × 109, `AveragePool` × 1, `BatchNormalization` × 3, `Concat` × 3, `Conv` × 62, `Div` × 19, `Erf` × 14, `HardSigmoid` × 6, `Identity` × 143, `MatMul` × 13, `MaxPool` × 1, `Mul` × 46, `Pow` × 5, `ReduceMean` × 16, `Relu` × 11, `Reshape` × 8, `Shape` × 4, `Sigmoid` × 5, `Slice` × 8, `Softmax` × 3, `Sqrt` × 5, `Squeeze` × 8, `Sub` × 5, `Transpose` × 9, `Unsqueeze` × 1.

- Dynamic tensors after ONNX shape inference: 494.
- Shape/metadata nodes: 15.
- Constant-fold candidates: 0.
- Identity nodes removable by the converter: 143.
- Conv + BatchNormalization fusion candidates: 3.
- Estimated heavy-operator work: 11.292 GFLOPs.
- FLOPs by heavy operator: Conv 11.005 GFLOPs, MatMul 287.386 MFLOPs.

## REC-first runtime surface

The exact REC graph contains `Add`, `AveragePool`, `BatchNormalization`, `Concat`, `Conv`, `Div`, `Erf`, `HardSigmoid`, `Identity`, `MatMul`, `MaxPool`, `Mul`, `Pow`, `ReduceMean`, `Relu`, `Reshape`, `Shape`, `Sigmoid`, `Slice`, `Softmax`, `Sqrt`, `Squeeze`, `Sub`, `Transpose`, `Unsqueeze`.

The REC converter removes verified Identity aliases. The remaining executable
operator requirement derived from this exact model is:

`Add`, `AveragePool`, `Concat`, `Conv`, `Div`, `Erf`, `HardSigmoid`, `MatMul`, `MaxPool`, `Mul`, `Pow`, `ReduceMean`, `Relu`, `Reshape`, `Shape`, `Sigmoid`, `Slice`, `Softmax`, `Sqrt`, `Squeeze`, `Sub`, `Transpose`, `Unsqueeze`.

This analysis-derived set is not a frozen LWM contract. Runtime implementation
status and independent reference-test coverage are tracked in `scalar-kernels.md`.

## REC dynamic-width propagation

REC input: `x` [DynamicDimension.0, 3, 48, DynamicDimension.1].

REC output: `fetch_name_0` [DynamicDimension.0, Reshape_498_o0__d2, 18710].

The batch, channel, and height constraints remain fixed for the MVP while input width
is symbolic. ONNX shape inference propagates width through convolution/pooling and into
the sequence dimension. Dynamic output-producing nodes by operator are: `Add` × 109, `AveragePool` × 1, `BatchNormalization` × 3, `Concat` × 1, `Conv` × 62, `Div` × 19, `Erf` × 14, `HardSigmoid` × 6, `Identity` × 138, `MatMul` × 13, `MaxPool` × 1, `Mul` × 46, `Pow` × 5, `ReduceMean` × 16, `Relu` × 11, `Reshape` × 8, `Sigmoid` × 5, `Slice` × 6, `Softmax` × 3, `Sqrt` × 5, `Squeeze` × 7, `Sub` × 5, `Transpose` × 9. The converter must preserve only this PP-OCR-specific width expression; it must
not introduce a general symbolic-shape engine.

## Shape and converter-removable work

- `Identity` is structurally removable after verifying graph outputs and aliases.
- `Shape`-derived integer subgraphs are metadata candidates and should be evaluated offline
  whenever all required dimensions are fixed or expressible by the supported REC width rule.
- Initializer-only subgraphs are constant-fold candidates.
- Conv + BatchNormalization is a low-risk fusion candidate but requires FP32 equivalence tests.
- Data-path `Transpose`, `Squeeze`, `Unsqueeze`, `Reshape`, and arithmetic operations remain
  runtime work unless analysis proves that a specific node is metadata-only or foldable.

## Main compute conclusion

The representative FLOP estimate is dominated by convolution and matrix multiplication.
The first performance work should therefore follow correctness-tested scalar Conv and MatMul;
metadata operators should be minimized in the converter. No kernel is justified solely because
it exists in ONNX—the exact model/operator matrix above is the requirement source.

## Runtime handoff requirements

1. Lock conversion to the exact verified REC model identity and opset.
2. Remove only structurally verified aliases and retain every required data-path node.
3. Require an independent NumPy or ONNX reference test for every LWM operator.
4. Compare complete-graph output before exposing a public inference call; enforce this with the private executor reference test.

## Fixed-width conversion checkpoint

The analysis-only converters `tools/convert_medium_rec_experimental.py` and
`tools/convert_medium_det_experimental.py` now materialize fixed-shape LWM
graphs without changing the production converter. REC supports widths `192,
320, 480, 640, 960`; the native `rec-graph-driver` output was compared with
ONNX Runtime using the deterministic input used by the Small REC gate. All five
widths produced finite output with the expected shape `[1, width / 8, 18710]`.
DET was checked at `320x320`, `640x640`, and `640x960`, with output shape
`[1,1,H,W]` at each shape.

The current analysis gate is deliberately separate from the Small production
gate: `max_abs_error <= 3e-4`, `mean_abs_error <= 1e-6`, and no more than
`1e-4` of values above the `1e-4` reference threshold. The observed maximum
absolute error was `2.5361776e-4` at width 960; mean absolute error stayed below
`6.7e-9` for every width. This is sufficient evidence for continued conversion
work, not a production-model promotion. The DET gate used the existing
`1e-4` absolute-error limit and all three shapes passed; its largest observed
absolute error was `3.53e-7`.

Reproduce the checkpoint with:

```text
python tools/run_medium_rec_validation.py \
  --model models/ppocrv6-medium/rec.onnx \
  --build-dir build \
  --output-dir build-model-foundation/medium-analysis/validation
```

For DET, run:

```text
python tools/run_medium_det_validation.py \
  --model models/ppocrv6-medium/det.onnx \
  --build-dir build \
  --output-dir build-model-foundation/medium-analysis/det-validation
```

The dynamic DET and REC prototypes also compose with the existing pure-C OCR
driver and the shared Tiny CLS LWM. On the repository's 500x500 sample they
returned 16 lines, with the first line `纯臻营养护发素`; this is a complete
pipeline checkpoint only, not a Medium production-support claim. Reproduce it
with:

```text
python tools/run_medium_ocr_validation.py \
  --det-model models/ppocrv6-medium/det.onnx \
  --rec-model models/ppocrv6-medium/rec.onnx \
  --cls-model build/models/cls.lwm \
  --dictionary models/ppocrv6-shared/PP-OCRv6_small_rec_dict.txt \
  --sample build/models/sample.ppm \
  --build-dir build \
  --output-dir build-model-foundation/medium-analysis/full-ocr
```

The generated LWM files and validation outputs are temporary build artifacts;
they are intentionally not part of the released model catalog yet.
